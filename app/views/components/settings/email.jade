h2 Change email
form(name='changeEmail', method='post', action='/settings/email')
  .form__item
    input(type='email', name='email', placeholder='Email', value=req.user.email)
  .form__item.hidden
    input(type='hidden', name='_csrf', value=req.csrfToken())
  .form__item
    button(type='submit') Confirm
  if message
    .form__item
      p #{message}
